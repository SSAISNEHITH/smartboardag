# Production environment variables
# Set VITE_API_URL in Railway Service Variables (e.g. https://backend-production-xxxx.up.railway.app)
# If left unset in Railway, it will default to empty / relative or configured fallback
VITE_API_URL=
