spring.application.name=backend

# ─── Database Configuration ──────────────────────────────────────────────────
# Supports Railway, Render, TiDB, Aiven, or H2 in-memory fallback
spring.datasource.url=${SPRING_DATASOURCE_URL:${JDBC_DATABASE_URL:${DATABASE_URL:jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:thrisual}?useSSL=${MYSQL_USE_SSL:false}&serverTimezone=UTC&allowPublicKeyRetrieval=true}}}
spring.datasource.username=${MYSQLUSER:${MYSQL_USER:root}}
spring.datasource.password=${MYSQLPASSWORD:${MYSQL_PASSWORD:24HR1A05F0@saisnehithbackbencher}}
spring.datasource.driverClassName=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:com.mysql.cj.jdbc.Driver}

spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.MySQLDialect}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# ─── JWT ────────────────────────────────────────────────────────────────────
jwt.secret=${JWT_SECRET:94a08da1fecbb6e8b46990538c7b50b2b8c56e7e4a1a6b0c2e3d9e8f4a7c1b2c}
jwt.expiration=86400000

# ─── File Storage ───────────────────────────────────────────────────────────
file.upload-dir=storage/

# ─── Server Port (Render & Railway inject PORT automatically) ───────────────
server.port=${PORT:8080}
